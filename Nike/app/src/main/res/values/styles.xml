<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>



    <!--Definition item title-->
    <style name="definitionItemTitle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Large</item>
        <item name="android:gravity">start</item>
        <item name="android:paddingBottom">@dimen/default_text_padding</item>

    </style>

    <!--Definition item example text -->
    <style name="definitionItemBody">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Small</item>
        <item name="android:gravity">start</item>
        <item name="android:paddingTop">@dimen/default_text_padding</item>

    </style>


    <!--No Data Available -->
    <style name="definitionListNoDataAvailable">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Display1</item>
        <item name="android:gravity">center</item>
        <item name="android:text">@string/no_data_available</item>
        <item name="android:paddingTop">@dimen/default_text_padding</item>

    </style>


</resources>
